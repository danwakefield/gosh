case a in
	a) echo "SUCCESS"
	;;
	b) echo "FAIL"
esac

case b in
	a) echo "FAIL"
	;;
	b) echo "SUCCESS"
esac

case xyz in
	xyz) echo "SUCCESS"
	;;
	*) echo "FAIL"
esac

case xyz in
	*) echo "SUCCESS"
esac

case bb in
	[a-c][a-c]) echo "SUCCESS"
	;;
esac
